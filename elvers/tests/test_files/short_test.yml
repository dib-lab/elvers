# testing yml 

# this sets the output directory name:
basename: 'test'

# this describes the samples:
get_data:
  samples: "reads/test.samples.tsv"

# location of test input assembly 
get_reference:
  reference: "reference/transcriptome.fasta"
assemblyinput:
  assembly: "reference/transcriptome.fasta"
