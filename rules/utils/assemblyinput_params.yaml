assemblyinput:
  eelpond_params:
    outdir: assembly
    extensions:
      base:
        - '.fasta'
        - '.fasta.gene_trans_map'
  # for user input
  program_params:
    assembly: 'name.fasta'
    gene_trans_map:  'name.fasta.gene_trans_map'
    assembly_extension: '_input'
