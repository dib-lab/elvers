$schema: "http://json-schema.org/draft-06/schema#"

description: trimmomatic params file

type: object

http://json-schema.org/draft-06/schema#"required:
  - trimmomatic 

properties:
  trimmomatic:
    type: string
      trim_cmd:
        type: string
        pattern: "^"ILLUMINACLIP:*"
  adapter:
    type: string
    pattern: "^[ACGT]+$"
  required:
    - trim_cmd
    - adapter
  
